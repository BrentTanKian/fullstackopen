sequenceDiagram
    participant browser
    participant server

    Note right of browser: User submits the form on /spa

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 Created (JSON response)
    deactivate server

    Note right of browser: JS updates the notes list dynamically<br>without reloading or redirecting

    browser-->>browser: Render new note on the page
